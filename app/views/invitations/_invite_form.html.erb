<%= form_for @invitation do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id  %>
  <%= f.text_field :email, class:"search-query", placeholder: "Input email to invite" %>
  <%= f.submit "Invite new friend", class: "btn btn-primary" %>
<% end %>